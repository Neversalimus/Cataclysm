name: NCMM Feed Integrity

on:
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'
  push:
    paths:
      - 'ncmm-platform/feed/**'

permissions:
  contents: read

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  audit-feed:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout NCMM repository
        uses: actions/checkout@v4

      - name: Validate repository text encoding
        shell: pwsh
        run: |
          ./ncmm-platform/ci/Test-TextEncoding.ps1 -RepoRoot "$env:GITHUB_WORKSPACE"

      - name: Feed auditor self-test
        shell: pwsh
        run: |
          ./ncmm-platform/ci/Test-FeedIntegrity.ps1 -SelfTest

      - name: Audit published certified-host feed
        shell: pwsh
        run: |
          $revision = (./ncmm-platform/ci/Get-PatchRevision.ps1 -RepositoryRoot ./ncmm-platform).Trim()
          ./ncmm-platform/ci/Test-FeedIntegrity.ps1 `
            -FeedPath ./ncmm-platform/feed/index.json `
            -RejectedPath ./ncmm-platform/feed/rejected.json `
            -ExpectedRuntimeVersion '0.6.5' `
            -ExpectedPatchRevision $revision `
            -Repository '${{ github.repository }}' `
            -Online
